{
  "buildCommand": "prisma generate && npm run build",
  "functions": {
    "app/api/**": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_CLI_BINARY_TARGETS": "native,rhel-openssl-1.0.x"
  }
} 